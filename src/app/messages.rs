//! Legacy re-exports for protocol types.
//!
//! The canonical definitions now live under `render::protocol`; this module exists temporarily to
//! avoid rippling imports while the render coordinator is being extracted.

pub use crate::render::protocol::*;
